# Build files
/export

# Build file that is needed sometimes in case language server does not initialize after cloning
build.hxml

# local copy of dependencies
/.haxelib

# local grafana dashboard files
metrics/data/

# potential sensitive datasource information
metrics/.env

# potential ldtk backups we don't want in the repo
/backups

# FMOD
fmod/.cache/
fmod/.unsaved/
# haxefmod integration
fmod/Cached*

# error files
dump/

# misc
.VSCodeCounter/
.DS_Store
